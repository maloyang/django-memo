from django.contrib import admin
from .models import Vendor, Food, VendorAdmin

# Register your models here.
#admin.site.register(Vendor, VendorAdmin) #-->已有models.py中的@admin.register(Vendor)
#admin.site.register(Food)
